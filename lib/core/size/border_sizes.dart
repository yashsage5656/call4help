import 'package:flutter/material.dart';

class BorderSizes {
  // buttons control
  static final buttonBorderRadius = BorderRadius.circular(8);
  static final buttonPadding = const EdgeInsets.symmetric(horizontal: 20, vertical: 12);
}
